name: REPLACE_ME
owners: []
envs:
  - stawi-dev
# Which cloud accounts supply resources (project, Neon org, secrets).
# Registries: config/gcp-accounts.yaml, config/neon-accounts.yaml
gcp:
  account: labs
neon:
  account: labs
runtime: cloudrun
