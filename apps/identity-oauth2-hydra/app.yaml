name: identity-oauth2-hydra
owners:
  - identity-team
envs:
  # Prod-first: stand up stawi-dev later when resources allow
  - stawi-prod
gcp:
  account: identity
neon:
  account: identity
supabase:
  account: identity
runtime: cloudrun
domain: identity
