name: api-gateway
owners:
  - platform
envs:
  - stawi-prod
gcp:
  account: api
# No Neon — path-based Global HTTPS LB front door for api.stawi.org.
# Backends (NEG + backend service) live in each domain project; frontend here.
runtime: cloudrun
domain: api
version: 1
