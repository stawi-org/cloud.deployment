# Mirrored from GHCR into project AR (Cloud Run cannot pull private GHCR cache).
image = "europe-west9-docker.pkg.dev/stawi-platform/apps/service-profile-devices:v1.53.7"

# Public edge (docs/PUBLIC_EDGE_DNS.md + config/public-edge.yaml)
public_hostname       = "devices.stawi.org"
